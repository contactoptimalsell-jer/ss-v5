{
  "buildCommand": "rm -rf node_modules .next .vercel && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "rm -rf node_modules && npm install --no-cache",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node"
    }
  }
}
